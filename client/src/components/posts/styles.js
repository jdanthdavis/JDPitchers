import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  floatDiv: {
    float: 'left',
    paddingRight: '.3rem',
  },
  progress: {
    position: 'absolute',
    top: '50%',
    left: '50%'
  }
}));